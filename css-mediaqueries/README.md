# Media Queries
An exercise in media queries.

1. Apply the CSS Grid areas layout
2. Use media queries to make sure your layout works at different screen resolutions
3. If you have extra time, try implementing a theme switcher using jQuery
